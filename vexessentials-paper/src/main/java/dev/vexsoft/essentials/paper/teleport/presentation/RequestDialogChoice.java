package dev.vexsoft.essentials.paper.teleport.presentation;

/** Normalizes the possible outcomes of the teleport request dialog. */
public enum RequestDialogChoice {
  ACCEPT,
  DENY,
  CLOSED,
  UNAVAILABLE
}
